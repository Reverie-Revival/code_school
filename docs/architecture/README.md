# Architecture notes

System design and data-flow docs for Code School go here as the build progresses (Pyodide integration, Supabase schema/RLS setup, progress-sync flow, GitHub Pages deploy pipeline, etc.).

For now, the "why" behind each major choice lives in [docs/decisions/](../decisions/) as ADRs — this folder is for detailed system-design/data-flow write-ups once there's enough built to document (e.g. the Supabase schema once it exists).
